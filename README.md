# sevenangelsmassage
This will be my future website but right now this will handle redirection to Google Sites.
Currently this mainly has Primary and fallback redirect service for sevenangelsmassage.com (apex → www), used with multi-provider DNS (Cloudflare + deSEC).
